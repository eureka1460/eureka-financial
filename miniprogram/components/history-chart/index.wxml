<view class="chart-card" wx:if="{{bars.length > 0}}">
  <text class="chart-title">{{title}}</text>
  <view class="chart-scroll">
    <canvas type="2d" id="chart-canvas" class="chart-canvas" style="width: {{chartWidth || 600}}px;"></canvas>
  </view>
</view>
