.chart-card {
  margin: var(--spacing-sm) 0;
  padding: var(--spacing-sm);
  background: var(--color-surface);
  border-radius: var(--radius-sm);
}
.chart-title { display: block; margin-bottom: 4rpx; font-size: 24rpx; }
.chart-scroll { width: 100%; overflow-x: scroll; -webkit-overflow-scrolling: touch; }
.chart-canvas { display: block; height: 320rpx; min-width: 600px; }
