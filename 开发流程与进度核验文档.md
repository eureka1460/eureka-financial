# A股财报选股与估值小程序 —— 开发流程与进度核验文档

> **用途**：跨任务 / 跨 AI 会话的开发进度追踪。每完成一个子任务，标记 ✅
> **配套文档**：`系统架构与后端 PRD 文档.md`（详细设计）
> **创建日期**：2026-07-26
> **最后更新**：2026-08-07（上线前最终测试）

---

## 开发阶段全景

```
[阶段1: 项目骨架] → [阶段2: 数据库模型] → [阶段3: ETL数据抓取] → [阶段4: 核心API] → [阶段5: 筛选+估值] → [阶段6: 鉴权+上线]
    1-2天              1-2天                3-5天                2-3天              3-5天              2-3天
```

---

## 阶段 1：项目骨架搭建

> **目标**：FastAPI 项目能启动，数据库能连接，目录结构就绪。

### 1.1 创建目录结构

- [x] 创建 `D:\尤里卡\backend\` 及所有子目录
- [x] 创建 `data/` 目录并添加 `.gitkeep`
- [x] 创建所有 `__init__.py` 文件

**验收标准**：目录结构与 PRD 文档第 9 节一致。

---

### 1.2 安装依赖

- [x] 编写 `requirements.txt`
- [x] `pip install -r requirements.txt` 成功

**依赖清单**：

```
# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 数据库
sqlalchemy>=2.0.0

# 数据抓取与处理
akshare>=2.0.0
pandas>=2.2.0
numpy>=2.0.0

# 缓存与重试
diskcache>=5.0.0
tenacity>=9.0.0
```

**验收标准**：`pip list` 列出上述所有包，无报错。

---

### 1.3 配置文件 `core/config.py`

- [x] 创建 `Settings` 类（pydantic-settings），包含：
  - `DATABASE_URL`：默认 `sqlite:///./data/eureka.db`
  - `CACHE_DIR`：默认 `./data/cache`
  - `CACHE_TTL_HISTORICAL`：默认 21600（6 小时）
  - `ETL_RATE_LIMIT_CALLS`：默认 4
  - `ETL_RATE_LIMIT_PERIOD`：默认 1.0

**验收标准**：`from app.core.config import settings` 可正常导入并读取默认值。

---

### 1.4 数据库连接 `core/database.py`

- [x] 创建 SQLAlchemy `engine`（启用 SQLite WAL 模式）
- [x] 创建 `SessionLocal` 工厂
- [x] 创建 `Base` 声明基类
- [x] 创建 `get_db` FastAPI 依赖函数

**验收标准**：`Base.metadata.create_all(engine)` 不报错。

---

### 1.5 FastAPI 入口 `main.py`

- [x] 创建 FastAPI app 实例
- [x] 注册健康检查路由 `GET /` 或 `GET /api/v1/health`
- [x] 配置 CORS 中间件
- [x] 注册自定义异常处理器（占位，可后续补充）

**验收标准**：`uvicorn app.main:app --reload` 启动成功，浏览器访问返回 `{"code":200,"message":"ok"}`。

---

### 1.6 异常类 `core/exceptions.py`

- [x] `DataFetchException` — 数据源不可用
- [x] `StockNotFoundException` — 股票代码不存在
- [x] `DataValidationException` — 数据校验失败
- [x] `ScreeningExpressionError` — 筛选表达式解析错误（占位）
- [x] `ValuationParameterError` — 估值参数不合法

**验收标准**：可正常 import，`raise StockNotFoundException("600000")` 能被全局异常处理器捕获并返回 404 JSON。

---

## 阶段 2：数据库模型设计

> **目标**：4 张核心表通过 SQLAlchemy 创建，Alembic 迁移可用。

### 2.1 `stocks` 表模型 `models/stocks.py`

- [x] 字段完全对齐 PRD 2.1 节
- [x] symbol 为主键
- [x] `__tablename__ = "stocks"`

**验收标准**：Python 中执行 `stocks.__table__.create(engine)` 成功建表，`sqlite3` 查看字段完整。

---

### 2.2 `financial_statements` 表模型 `models/financials.py`（核心）

- [x] 字段完全对齐 PRD 2.2 节（~60 个字段）
- [x] 唯一约束：`(symbol, report_date, report_type)`
- [x] 索引：symbol, report_date, fiscal_year, report_type

**验收标准**：成功建表，`PRAGMA table_info(financial_statements)` 显示全部字段。唯一约束生效（重复插入报错）。

---

### 2.3 `financial_indicators` 表模型 `models/indicators.py`

- [x] 字段完全对齐 PRD 2.3 节（~24 个计算指标）
- [x] 唯一约束：`(symbol, report_date, report_type)`

**验收标准**：成功建表。

---

### 2.4 `sync_log` 表模型 `models/sync.py`

- [x] 字段对齐 PRD 2.4 节
- [x] status 默认 'running'

**验收标准**：成功建表。

---

### 2.5 `reporting_calendar` 表模型（与 sync.py 合并或独立）

- [x] 字段对齐 PRD 2.5 节
- [x] 预置 A 股财报截止日数据（可用 seed 脚本）

**验收标准**：成功建表 + 预置数据可查询。

---

### 2.6 数据库初始化脚本 `scripts/init_db.py`

- [x] `import` 所有模型
- [x] 调用 `Base.metadata.create_all(engine)`
- [x] 如有预置数据一并写入

**验收标准**：`python scripts/init_db.py` 执行后，SQLite 文件中存在全部 5 张表，且表结构正确。

---

## 阶段 3：ETL 数据抓取模块（核心阶段）

> **目标**：能从 akshare 抓取真实 A 股财报数据，清洗后正确写入数据库。

### 3.1 列名映射 `services/etl/column_mapping.py`

- [x] 建立 akshare 英文缩写 → 数据库英文字段名的映射字典
- [x] 覆盖三张报表全部字段（80+ 条映射，英文缩写列名）
- [x] 适配东方财富主源 + 新浪财经备用源

**验收标准**：传入 akshare 返回的 DataFrame，`df.rename(columns=MAPPING)` 后字段名与数据库模型一致。

---

### 3.2 数据抓取器 `services/etl/fetcher.py`

- [x] 实现 `RateLimiter` 类（Token Bucket，调用间隔 0.5~1.5s 随机）
- [x] 实现 `DataFetcher` 类：
  - [x] `fetch_stock_list()` → 返回全 A 股列表 DataFrame
  - [x] `fetch_balance_sheet(symbol)` → 资产负债表
  - [x] `fetch_income_statement(symbol)` → 利润表（单季度版 `_by_quarterly_em`）
  - [x] `fetch_cash_flow(symbol)` → 现金流量表（单季度版 `_by_quarterly_em`）
  - [x] `fetch_all_for_stock(symbol)` → 一次获取三张表
- [x] 每个方法装饰 `@retry`（tenacity：5次，指数退避1s→60s）
- [x] 每个方法装饰 `@rate_limited`
- [x] 集成 diskcache（缓存命中直接返回）
- [x] L4 备用源 fallback 逻辑

**验收标准**：

1. `fetcher.fetch_stock_list()` 返回 ~5000 行，包含 symbol 和 name
2. `fetcher.fetch_all_for_stock("600519")` 返回三张非空 DataFrame
3. 连续调用同一股票两次，第二次命中缓存（日志可见）
4. 断网后调用 → 重试 5 次 → 抛出 `DataFetchException`

---

### 3.3 数据清洗器 `services/etl/cleaner.py`

- [x] 实现 `DataCleaner` 类：
  - [x] `clean_balance_sheet(df)` — 列名映射 + 单位检测转换 + 空值处理
  - [x] `clean_income_statement(df)` — 单季度数据清洗 + 日期解析
  - [x] `clean_cash_flow(df)` — 单季度数据清洗 + 日期解析
  - [x] `merge_statements(...)` — 三表合并为完整宽表行
- [x] 单位检测逻辑：如果 total_assets 中位数 > 1e14，判定为"万元"→ 乘以 10000
- [x] report_type 推断逻辑（从日期推断 annual/semi_annual/q1/q3）
- [x] 数据校验：总资产 > 0；|总资产 − 总负债 − 净资产| / 总资产 < 1%

**验收标准**：

1. 输入 akshare 原始 DataFrame → 输出列名为数据库英文字段名
2. 单位检测有日志输出（"检测到单位：元/万元"）
3. 非法数据（total_assets = 0 或负）被识别并日志警告

---

### 3.4 数据入库器 `services/etl/loader.py`

- [x] 实现 `DataLoader` 类：
  - [x] `upsert_stocks(df)` — 批量 upsert 股票信息
  - [x] `upsert_financials(records)` — 批量 upsert 财务报表（ON CONFLICT 更新）
  - [x] `compute_and_store_indicators(symbol)` — 计算 ROE/ROA/毛利率/增长率…并写入 indicators 表
- [x] upsert 策略：SQLAlchemy `insert().on_conflict_do_update()`
- [x] 增长率计算：通过上年同期 prev_map 对比计算

**验收标准**：

1. 同一只股票同一报表期入库两次，第二次不产生重复行，updated_at 更新
2. `compute_and_store_indicators("600519")` 后，indicators 表中有对应的 ROE/毛利率等计算值
3. 人工抽查：ROE = net_profit_attr_parent / ((本期total_equity + 上期total_equity) / 2)

---

### 3.5 ETL 编排器 `services/etl/scheduler.py`

- [x] 实现 `ETLOrchestrator` 类：
  - [x] `sync_all_stocks(years=5, symbols=None)` — 全量同步
  - [x] `incremental_sync()` — 增量同步（占位，逻辑框架已就绪）
- [x] 全量同步流程完整
- [x] 异常处理：单只股票抓取失败不中断全流程，记录到 stocks_failed
- [x] 进度日志：每 50 只输出一次

**验收标准**：

1. `orchestrator.sync_all_stocks(years=1, symbols=["600519","000858"])` 成功插入数据
2. sync_log 中有完整的执行记录
3. 其中一只股票抓取失败时，另一只仍然成功，sync_log.status = 'partial'

---

### 3.6 ETL 手动触发脚本 `scripts/run_etl.py`

- [x] 命令行参数：`--symbols`, `--years`, `--sync-type`, `--dry-run`, `--report-types`
- [x] `python scripts/run_etl.py --symbols 600519 --years 3` 执行成功，数据库中有 600519 的最近 3 年全部报表期数据（含 Q1/Q2/Q3/年报）

**验收标准**：`python scripts/run_etl.py --symbols 600519 --years 3` 执行成功，数据库中有 600519 的最近 3 年年报数据。

---

### 3.7 阶段 3 整体验收

> **里程碑：能成功抓取单只股票完整季度财报，资产负债表为时点快照，利润表和现金流为单季度独立数据。**

- [x] `python scripts/run_etl.py --symbols 600519 --years 3` 执行成功
- [x] `financial_statements` 表中有 600519 的 12 条报表（3 年 × 4 季度）
- [x] `financial_indicators` 表中有对应的计算指标（ROE/ROA/毛利率/YoY/流动比率等）
- [x] 手工抽查 600519 的 ROE/毛利率与公开数据吻合（误差 < 5%）
- [x] FY2025 四个单季度营收之和 = 年报总营收（数据一致性验证通过）

---

## 阶段 4：核心 API 实现

> **目标**：提供股票查询和财务数据查询的 REST API。

### 4.1 通用模块 `schemas/common.py`

- [ ] `APIResponse[T]` 泛型包装
- [ ] `PaginatedData[T]` 分页包装
- [ ] `PaginationParams` 请求参数依赖（page, page_size）

**验收标准**：任何接口返回的 JSON 结构符合 PRD 1.3 节规范。

---

### 4.2 股票列表接口 `routers/stocks.py`

- [ ] `GET /api/v1/stocks`
  - [ ] keyword 模糊搜索（symbol + name LIKE）
  - [ ] industry 精确筛选
  - [ ] 分页（默认 page=1, page_size=20，最大 100）
  - [ ] 返回 SymbolSchema（symbol, name, market, industry, list_date, total_shares）

**验收标准**：

1. `GET /api/v1/stocks` → 返回前 20 只（按 symbol 排序）
2. `GET /api/v1/stocks?keyword=茅台` → 返回贵州茅台
3. `GET /api/v1/stocks?keyword=600519` → 返回贵州茅台
4. `GET /api/v1/stocks?page=2&page_size=10` → 返回第 11~20 条，total 为全量
5. `GET /api/v1/stocks?page_size=200` → 报 400 错误（超过最大限制）

---

### 4.3 股票详情接口 `routers/stocks.py`

- [ ] `GET /api/v1/stocks/{symbol}`
  - [ ] 404 处理：股票不存在 → `StockNotFoundException`
  - [ ] 关联查询 `financial_statements` + `financial_indicators` 取最新一条
  - [ ] 返回完整 StockDetailSchema

**验收标准**：

1. `GET /api/v1/stocks/600519` → 返回股票信息 + 最新 ROE/毛利率/营收增速
2. `GET /api/v1/stocks/999999` → 返回 `{"code":404,"message":"股票代码不存在"}`

---

### 4.4 财务历史接口 `routers/financials.py`

- [ ] `GET /api/v1/stocks/{symbol}/financials`
  - [ ] `?report_type=annual` 筛选报表类型
  - [ ] `?years=5` 限制返回年数
  - [ ] 返回所有 `financial_statements` + `financial_indicators` 字段

**验收标准**：

1. `GET /api/v1/stocks/600519/financials` → 返回所有报表期数据
2. `GET /api/v1/stocks/600519/financials?report_type=annual&years=3` → 返回最近 3 份年报

---

### 4.5 指标列表接口 `routers/financials.py`

- [ ] `GET /api/v1/indicators`
  - [ ] 返回全部可筛选指标的 field / chinese_name / unit / category / is_ratio
  - [ ] 数据来源：硬编码的指标元数据配置文件

**验收标准**：返回数据与 PRD 4.2 节响应示例结构一致，覆盖全部 ~20 个计算指标 + 全部 financial_statements 字段。

---

### 4.6 数据同步接口 `routers/data.py`

- [ ] `POST /api/v1/data/sync`
  - [ ] 接收 JSON Body：sync_type, years, symbols, report_types
  - [ ] 参数校验（years 范围 1-10，symbols 最多 100 个）
  - [ ] 异步后台任务启动 ETL Orchestrator
  - [ ] 返回 job_id
- [ ] `GET /api/v1/data/sync/status`
  - [ ] 返回最近 10 条 sync_log 记录

**验收标准**：

1. `POST /api/v1/data/sync {"symbols":["600519"],"years":2}` → 返回 job_id
2. `GET /api/v1/data/sync/status` → 返回包含该 job 的状态

---

### 4.7 阶段 4 整体验收

> **里程碑：所有 P0 接口可正常调用，返回格式正确，数据真实。**

- [ ] `GET /api/v1/stocks?keyword=茅台` → 正确匹配
- [ ] `GET /api/v1/stocks/600519` → 返回完整最新财务数据
- [ ] `GET /api/v1/stocks/600519/financials?report_type=annual` → 返回所有年报历史
- [ ] `GET /api/v1/indicators` → 返回完整指标列表
- [ ] `POST /api/v1/data/sync` + `GET /api/v1/data/sync/status` → 同步流程正常
- [ ] Swagger 文档可访问（FastAPI 自动生成）

---

## 阶段 5：筛选引擎 + 估值模型（后续）

> **目标**：实现自定义指标筛选和 DCF/DDM 估值计算。
> **状态**：⏸️ 设计完成，待 v2.1 开发

### 5.1 筛选表达式解析器 `services/screener.py`

- [ ] 词法分析器（tokenizer）
- [ ] 语法分析器（parser）→ AST
- [ ] AST → SQLAlchemy Query 编译
- [ ] 连续 N 年逻辑实现

### 5.2 筛选接口 `routers/screener.py`

- [ ] `POST /api/v1/screener/search`

### 5.3 DCF 估值 `services/valuation.py`

- [ ] 两阶段 DCF 计算
- [ ] 自动参数填充逻辑

### 5.4 DDM 估值 `services/valuation.py`

- [ ] 两阶段 DDM 计算
- [ ] 自动参数填充逻辑

### 5.5 估值接口 `routers/valuation.py`

- [ ] `POST /api/v1/valuation/dcf`
- [ ] `POST /api/v1/valuation/ddm`

---

## 阶段 6：鉴权 + 上线准备（后续）

> **状态**：⏸️ 设计完成，待 v2.2 开发

- [ ] 微信登录接口 `POST /api/v1/auth/wechat-login`
- [ ] JWT 签发与验证中间件
- [ ] 接口鉴权注入（Dependency）
- [ ] 限流中间件上线
- [ ] 生产环境迁移（SQLite → PostgreSQL）
- [ ] HTTPS 证书配置
- [ ] 微信小程序域名白名单配置

---

## 快速启动清单

新会话 / 新 AI 接手时，按此顺序恢复上下文：

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 阅读本文档 | 了解整体进度和待办 |
| 2 | 阅读 `系统架构与后端 PRD 文档.md` | 了解详细设计 |
| 3 | `cd D:\尤里卡\backend` | 进入项目目录 |
| 4 | `pip install -r requirements.txt` | 安装依赖 |
| 5 | `python scripts/init_db.py` | 初始化数据库 |
| 6 | 检查 `app/main.py` 是否可启动 | `uvicorn app.main:app --reload` |
| 7 | 根据本文件"待完成项"继续开发 | 从第一个未勾选的 `[ ]` 开始 |

---

## 进度汇总

| 阶段 | 状态 | 完成率 | 开始日期 | 完成日期 |
|------|------|--------|----------|----------|
| 阶段1：项目骨架 | ✅ 已完成 | 6/6 | 2026-07-26 | 2026-07-26 |
| 阶段2：数据库模型 | ✅ 已完成 | 6/6 | 2026-07-26 | 2026-07-26 |
| 阶段3：ETL 数据抓取 | ✅ 已完成 | 7/7 | 2026-07-26 | 2026-07-26 |
| 阶段4：核心 API | ✅ 已完成 | 5/5 | 2026-07-26 | 2026-07-26 |
| 阶段5：筛选+估值 | ✅ 已完成 | 5/5 | 2026-07-30 | 2026-07-30 |
| 阶段6：前端开发 | ✅ 已完成 | 4/4 | 2026-08-02 | 2026-08-07 |
| 阶段7：部署+MySQL | ✅ 已完成 | 3/3 | 2026-08-02 | 2026-08-05 |
| 阶段8：鉴权+上线 | ⏸️ 待上线 | 0/3 | — | — |

### 阶段6-7 新增内容

**前端（4 页面 + 3 组件 + 图表）**
- 首页：搜索 + 行业筛选 + 股票列表
- 个股页：多期数据对比表 + 点击指标弹出柱状图（年报/季报切换）
- 筛选页：多条件选股（指标选择器 + AND/OR + 连续N年）
- 估值页：DCF/DDM 双模型 + WACC 明细展开编辑
- 指标问号帮助组件（30+ 会计定义）

**数据与部署**
- MySQL 永久存储（云开发托管数据库）
- 云托管自动部署（Git Push → 自动构建）
- 同花顺预计算指标（ROE/ROA/毛利率/速动比率等，不再自行计算）
- 全量分批同步 + 季度增量更新 + 断点续传
- 云函数 API 代理

**关键决策**
- 财报数据源：东方财富 `_by_report_em`（年报=全年累计值）
- 指标数据源：同花顺 `stock_financial_abstract_ths`（预计算，覆盖所有季度）
- FCF = 经营CF + 投资CF净额

---

> **使用说明**：每完成一个 `[ ]` 子任务，将其改为 `[x]`，并更新上方进度汇总表格的完成率和日期。
> **切换 AI 时**：将本文档作为附件或上下文提供，新的 AI 即可快速了解项目状态。
